# Bugfix Workflow

Steps:
- Reproduce or encode the failing behavior.
- Add a regression test before or with the fix.
- Keep the fix limited to the failing contract.
- Record evidence and remaining risk.

Exit:
- Regression test fails before the fix or clearly targets the broken path, then passes.
