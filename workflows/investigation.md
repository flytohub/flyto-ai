# Investigation Workflow

Collect:
- Repo state, relevant files, impact graph, existing tests, and runtime assumptions.
- External docs only from primary sources when behavior could have changed.

Exit:
- Findings separate confirmed facts from assumptions.
